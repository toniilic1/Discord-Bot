Requires beautifulsoup, requests and pytest libraries to be installed.

Made on python 3.11

To run the tests, type 'pytest' in folders command line.
If you wish to see the scraped websites result, run igscrape.py or steamscrape.py, but uncomment the last line first to see printed out result.